[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "guardmcp-observability"
version = "0.1.0"
description = "GuardMCP guardmcp-observability � placeholder"
requires-python = ">=3.11"

[tool.hatch.build.targets.wheel]
packages = ["src/guardmcp_observability"]

