[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "guardmcp-audit"
version = "0.1.0"
description = "GuardMCP guardmcp-audit � placeholder"
requires-python = ">=3.11"

[tool.hatch.build.targets.wheel]
packages = ["src/guardmcp_audit"]

