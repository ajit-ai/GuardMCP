[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "guardmcp-context"
version = "0.1.0"
description = "GuardMCP guardmcp-context � placeholder"
requires-python = ">=3.11"

[tool.hatch.build.targets.wheel]
packages = ["src/guardmcp_context"]

