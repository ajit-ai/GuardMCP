[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "guardmcp-core"
version = "0.1.0"
description = "GuardMCP core domain models - GuardContext, GuardRequest, GuardDecision, GuardResult"
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[tool.hatch.build.targets.wheel]
packages = ["src/guardmcp_core"]
